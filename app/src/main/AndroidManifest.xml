<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.silentcodder.hospital">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:hardwareAccelerated="false"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hospital">
        <activity android:name=".PatientRegister_3" />
        <activity android:name=".PatientRegister_2" />
        <activity android:name=".PatientRegisterOtp" />
        <activity android:name=".PatientRegister_1" />
        <activity android:name=".PatientLoginOtp" />
        <activity android:name=".Patient.PatientMainActivity" />
        <activity android:name=".Counter.CounterMainActivity" />
        <activity android:name=".HospitalStaffLogin" />
        <activity android:name=".PatientLogin" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>